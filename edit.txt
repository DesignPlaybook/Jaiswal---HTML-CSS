
h2,
h3,
.price {
    font-family: "Poppins", serif;
    margin: 0;
}

h2 {
    color: #151618;
    font-size: 3.0625rem;
    font-weight: 700;
    line-height: 120%;
    margin: 2rem 0;
}

h3 {
    font-size: 2.1875rem;
    font-weight: 400;
    line-height: 120%;
    color: white;
}

.price {
    font-variant-numeric: lining-nums proportional-nums;
    line-height: 120%;
    font-size: 1.5625rem;
}

header p {
    font-size: 1.125rem;
}

p.small {
    color: var(--logo-blue1);
    font-size: 0.8125rem;
    font-weight: 700;
    text-transform: uppercase;
}

figure,
figure p.small {
    color: #fff;
}

figure h3+p {
    font-size: .8125rem;
}

figure .about-footer p.small {
    font-weight: 400;
    margin-bottom: 0;
}

img {
    height: 100%;
    width: 100%;
    display: block;
    object-fit: cover;
}

header {
    /* margin-bottom: 7.5rem; */
    max-width: 70%;
}

.gallery {
    display: grid;
    list-style: none;
    padding: 0;
    margin: 0;
    grid-gap: 2rem;
    grid-template-columns: repeat(auto-fit, minmax(20rem, 1fr));
    grid-auto-rows: 40rem;
    grid-auto-flow: dense;
}

figure {
    margin: 0;
    width: 100%;
    height: 100%;
    position: relative;
}

figurecaption {
    position: absolute;
    inset: 0;
    pointer-events: none;
    padding: 4rem 2.5rem;
    display: flex;
    flex-direction: column;
    justify-content: flex-end;
    gap: 2rem;
}

figurecaption .about-main {
    display: flex;
    flex-direction: column;
    justify-content: flex-end;
    gap: 2rem;
}

figure p {
    margin: 0;
}

figurecaption .about-footer {
    display: flex;
    justify-content: space-between;
    align-items: center;
}

figure .about-footer img {
    width: auto;
}

figure .about-main,
figure .about-footer {
    opacity: 0;
}

figure:hover .about-main,
figure:hover .about-footer {
    opacity: 1;
}

figurecaption {
    z-index: 1;
}

figurecaption::before {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(180deg, rgba(21, 22, 24, 0.24) 0%, #151618 100%);
    z-index: -1;
    opacity: 0;
    transition-property: opacity;
}

figure:hover figurecaption::before {
    opacity: 1;
}

figure a img {
    transform: scale(1.2);
    transition-property: transform;
}

figure {
    overflow: hidden;
}

figure:hover a img {
    transform: scale(1);
}

figure .about-main {
    transform: translate3d(0, -6rem, 0);
    transition-property: transform;
}

figure .about-footer {
    transform: translate3d(0, 6rem, 0);
    transition-property: transform;
}

figure:hover .about-main,
figure:hover .about-footer {
    transform: translate3d(0, 0, 0);
}

figure .about-footer svg {
    transform: translate3d(-3rem, 0, 0) rotate(360deg);
    transition-delay: var(--def-transition-duration);
    opacity: 0;
    transition-property: opacity, transform, transition-delay;
}

figure:hover .about-footer svg {
    transform: translate3d(0, 0, 0) rotate(0);
    opacity: 1;
}

figurecaption::before,
figure a img,
figure .about-main,
figure .about-footer,
figure:hover .about-main,
figure:hover .about-footer svg {
    transition-timing-function: ease-in-out;
    transition-duration: var(--def-transition-duration);
}
